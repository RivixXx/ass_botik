# Telegram-бот для компании "Навикон"

Корпоративный Telegram-бот с функциями поиска сотрудников и AI-ассистентом на базе OpenAI.

## 🚀 Возможности

- 🔍 **Поиск сотрудников** - интеллектуальный поиск по имени, должности, отделу, email
- 🤖 **AI-ассистент** - интеграция с OpenAI для ответов на общие вопросы
- 💾 **Персистентные сессии** - сохранение контекста диалога в базе данных
- 🔒 **Безопасность** - авторизация, rate limiting
- ⚡ **Производительность** - кэширование результатов поиска

## 📋 Требования

- Node.js 18+ 
- npm или yarn
- Telegram Bot Token
- OpenAI API Key

## 🛠️ Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd ass
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` в корне проекта:
```env
TELEGRAM_TOKEN=your_telegram_bot_token
OPENAI_API_KEY=your_openai_api_key

# Опциональные настройки
LOG_LEVEL=info
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_MAX_TOKENS=800
OPENAI_TEMPERATURE=0.2
SYSTEM_PROMPT=Ты — полезный корпоративный ассистент компании Навикон. Отвечай кратко, вежливо, на русском языке.

# Безопасность
ADMIN_USER_IDS=123456789,987654321  # Telegram user IDs администраторов через запятую
RATE_LIMIT_ENABLED=true
RATE_LIMIT_MAX_REQUESTS=10
RATE_LIMIT_WINDOW_MS=60000

# Сессии
MAX_HISTORY_MESSAGES=10
SESSION_CLEANUP_INTERVAL=3600000

# Кэширование
CACHE_ENABLED=true
CACHE_TTL=300000
CACHE_MAX_SIZE=100
```

4. Инициализируйте базу данных:
```bash
npx prisma generate
npx prisma migrate dev
npx prisma db seed
```

## 🚀 Запуск

### Режим разработки:
```bash
npm run dev
```

### Продакшен:
```bash
npm start
```

## 📦 Docker

### Сборка образа:
```bash
docker build -t navicon-bot .
```

### Запуск с Docker Compose:
```bash
docker-compose up -d
```

## 🎯 Команды бота

- `/start` - Приветствие
- `/employees` - Список всех сотрудников
- `/addemployee` - Добавить сотрудника (только для администраторов)
- `/clear` - Очистить контекст диалога

## 💡 Примеры использования

### Поиск сотрудников:
- "Михаил Зорин" - информация о сотруднике
- "Зорин должность?" - должность сотрудника
- "кто главный бухгалтер" - поиск по должности
- "директор" - найти директора
- "отдел продаж" - сотрудники отдела

### AI-ассистент:
Любые вопросы, не связанные с сотрудниками, обрабатываются AI-ассистентом с сохранением контекста диалога.

## 📁 Структура проекта

```
ass/
├── src/
│   ├── bot.js                    # Основной файл бота
│   ├── config/                   # Конфигурация
│   ├── db/                       # База данных
│   ├── middleware/               # Middleware (auth, rateLimit)
│   ├── plugins/                  # Плагины
│   │   └── employees/           # Плагин сотрудников
│   ├── services/                 # Сервисы (session)
│   └── utils/                    # Утилиты (cache, errors)
├── prisma/
│   ├── schema.prisma            # Схема базы данных
│   └── seed.js                  # Начальные данные
└── package.json
```

## 🔧 Конфигурация

Все настройки вынесены в `src/config/index.js` и могут быть переопределены через переменные окружения.

### Безопасность

Для добавления администраторов укажите их Telegram user IDs в переменной окружения `ADMIN_USER_IDS`:
```env
ADMIN_USER_IDS=123456789,987654321
```

Узнать свой user ID можно через бота [@userinfobot](https://t.me/userinfobot) в Telegram.

### Rate Limiting

По умолчанию включен rate limiting: 10 запросов в минуту на пользователя. Настройки можно изменить через переменные окружения.

## 🗄️ База данных

Проект использует SQLite с Prisma ORM. База данных находится в `prisma/dev.db`.

### Миграции:
```bash
# Создать новую миграцию
npx prisma migrate dev --name migration_name

# Применить миграции
npx prisma migrate deploy
```

### Seed:
```bash
npx prisma db seed
```

## 📝 Разработка

### Добавление нового плагина:

1. Создайте папку в `src/plugins/your-plugin/`
2. Создайте `index.js` с экспортом функции регистрации:
```javascript
export default function register(bot, { prisma, logger }) {
  bot.command('command', async (ctx) => {
    // Ваш код
  });
}
```

3. Подключите плагин в `src/bot.js`:
```javascript
import yourPlugin from './plugins/your-plugin/index.js';
yourPlugin(bot, { prisma, logger });
```

## 🔍 Отладка

Логирование настроено через pino. Уровень логирования можно изменить через переменную окружения `LOG_LEVEL`:
- `error` - только ошибки
- `warn` - предупреждения и ошибки
- `info` - информационные сообщения (по умолчанию)
- `debug` - отладочная информация

## 📄 Лицензия

ISC

## 👥 Авторы

Компания "Навикон"

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь, что все переменные окружения настроены правильно
3. Проверьте, что база данных инициализирована

